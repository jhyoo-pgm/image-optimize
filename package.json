{
  "name": "image-optimize-demo",
  "version": "1.0.0",
  "private": true,
  "description": "이미지 최적화 데모 페이지",
  "scripts": {
    "build:images": "node scripts/build-images.js",
    "serve": "npx --yes http-server . -p 8080 -c-1"
  },
  "devDependencies": {
    "sharp": "^0.33.5"
  }
}
